# Production deps for the hosted backend (Render). The desktop-only packages
# (waitress, pywebview) are intentionally omitted; gunicorn + Postgres + a CDN
# of static files replace them. Keep versions in sync with requirements.txt.
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0
django-filter==24.3
openpyxl==3.1.5
reportlab==4.2.5
Pillow==11.0.0
whitenoise==6.8.2
gunicorn==23.0.0
dj-database-url==2.3.0
psycopg2-binary==2.9.10
